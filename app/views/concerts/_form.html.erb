<%= form_with(model: concert, local: true) do |form| %>
  <% if concert.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(concert.errors.count, "error") %> prohibited this concert from being saved:</h2>

      <ul>
      <% concert.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :asistentes %>
    <%= form.number_field :asistentes %>
  </div>

  <div class="field">
    <%= form.label :date_concert %>
    <%= form.date_select :date_concert %>
  </div>

  <div class="field">
    <%= form.label :duration%>
    <%= form.number_field :duration %>
  </div>

  <div class="field">
    <%= form.label :group_id %>
    <%= form.select :group_id, options_for_select(@groups= Group.all.pluck(:name, :id)), prompt: "Seleciona.."%>
    
  </div>

  

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
